{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "noEmit": true,
        "jsx": "react-jsx",
        "baseUrl": ".",
        // Against the package's source, so `typecheck` does not need a build first.
        "paths": {
            "@a2anet/react": ["../../packages/react/src/index.ts"]
        }
    },
    "include": ["server/**/*", "src/**/*"],
    "exclude": ["node_modules"]
}
